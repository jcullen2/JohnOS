# Lesson: Cowork

Output quality is a function of **context richness** (files / templates /
connectors available to the session), **not prompt cleverness**.

- Point Cowork's working folder at `/os` so it sees /context and /lessons.
- Invest in the substrate (fixtures, templates, connector access) before tuning
  the prompt.
- Cowork owns judgment-bearing scheduled passes: inbound triage, Monday sourcing
  rank, the Friday retro (spec §8).

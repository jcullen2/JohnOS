# Archive

Resolved queue items land here, moved on resolution with the resolution noted
(spec §6). Resolutions are training data for the Friday retro. One file per item;
same schema as /queue but status: resolved | expired.
